apply plugin: 'com.android.library'

android {
    namespace 'com.mac.imlibrary'
    compileSdk = 34

    defaultConfig {
        minSdk = 21
        targetSdk = 34
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
    // Add dependencies if needed
}
